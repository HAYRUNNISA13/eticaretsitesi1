@model List<mvc_project.Models.UserMessage>
@{
    ViewData["Title"] = "📬 Mesajlarım";
}

<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow border-0">
                <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
                    <h4 class="mb-0"><i class="bi bi-chat-dots-fill"></i> Mesajlarım</h4>

                    @if (Model.Any())
                    {
                        <form asp-action="Clear" method="post" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-light">
                                <i class="bi bi-trash-fill"></i> Tümünü Temizle
                            </button>
                        </form>
                    }
                </div>

                <div class="card-body bg-light">
                    @if (Model.Any())
                    {
                        <div class="list-group">
                            @foreach (var message in Model)
                            {
                                <div class="list-group-item list-group-item-action d-flex justify-content-between align-items-start rounded-2 mb-2 shadow-sm bg-white">
                                    <div class="me-3">
                                        <i class="bi bi-info-circle-fill text-primary me-2"></i>
                                        <span>@message.Message</span>
                                    </div>
                                    <small class="text-muted">@message.CreatedAt.ToString("dd MMM yyyy HH:mm")</small>
                                </div>
                            }
                        </div>
                    }
                    else
                    {
                        <div class="alert alert-info text-center">
                            <i class="bi bi-inbox-fill me-2"></i>Hiç mesajınız yok.
                        </div>
                    }
                </div>
            </div>

            @if (TempData["Message"] != null)
            {
                <div class="alert alert-success alert-dismissible fade show mt-3" role="alert">
                    <i class="bi bi-check-circle-fill me-2"></i>@TempData["Message"]
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            }
        </div>
    </div>
</div>
